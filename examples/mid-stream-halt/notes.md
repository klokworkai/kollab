# mid-stream-halt/

Artifacts for Example 5 go here. See `../EXAMPLES-PLAN.md` for the goal, when to halt, and capture checklist.

Files to add:
- `session.jsonl` — raw JSONL from `~/.kollab/sessions/<id>.jsonl` (no export needed — session may not be complete)
- `notes.md` — session ID, which turn was interrupted, how much output was captured before halt, what the resume looked like
- `screenshots/` — interrupted turn with ⏸ and partial text, post-resume session with both C-1 and C-2 visible
