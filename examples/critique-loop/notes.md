# critique-loop/

Artifacts for Example 1 go here. See `../EXAMPLES-PLAN.md` for the goal, instructions, and capture checklist.

Files to add:
- `transcript.md` — markdown export from the UI
- `session.jsonl` — raw JSONL from `~/.kollab/sessions/<id>.jsonl`
- `notes.md` — session ID, session number, which turns were interesting, what the core disagreement was
- `screenshots/` — turn cards, reasoning block expanded, collapse-all view
