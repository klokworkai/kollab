# directive-injection/

Artifacts for Example 3 go here. See `../EXAMPLES-PLAN.md` for the goal, directive text, and capture checklist.

Files to add:
- `transcript.md` — markdown export from the UI
- `session.jsonl` — raw JSONL from `~/.kollab/sessions/<id>.jsonl`
- `notes.md` — session ID, which turn was interrupted, what the directive was, how Claude's response changed after resume
- `screenshots/` — DIRECTIVE card, interrupted turn with ⏸ marker, before/after Claude turns
