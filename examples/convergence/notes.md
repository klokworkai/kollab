# convergence/

Artifacts for Example 2 go here. See `../EXAMPLES-PLAN.md` for the goal, instructions, and capture checklist.

Files to add:
- `transcript.md` — markdown export from the UI
- `session.jsonl` — raw JSONL from `~/.kollab/sessions/<id>.jsonl`
- `notes.md` — session ID, session number, how many rounds to convergence, what Codex's AGREE said
- `screenshots/` — AGREE pill, convergence banner, goal card with export button
